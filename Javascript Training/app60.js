// The call stack
// The mechanism of JS interpreter uses to keep track of its placec in a script that calls multiple functions.
// How JS "knows" what funcion is currently being run and what functions are called from within that function, etc.
// The last thing you added will be the first thing that comes out.

// Call stack with dev tools - see separate page

// JS is single threaded meaning at any given point in time, that single JS thread is running at most one line of JS code.
// We have a way around this

// The browser does the work. 